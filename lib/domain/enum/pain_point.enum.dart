/// Pain points reconnus par l'utilisateur en onboarding (multi-select).
///
/// Sert uniquement pour le branding du flow + analytics — non persisté
/// dans `UserPreferencesEntity`.
enum PainPoint {
  pelliculeCemetery,
  reGoogle,
  notionHeavy,
  neverRevisit,
  forgetWhatIKnow,
  noTimelyReminder,
  llmMissOut,
}
